import "./Header.styles.scss";

const Header = (props) => {
  return (
    <header>
      <div className="header">
        <div>HONEST FOOD</div>
      </div>
    </header>
  );
};

export default Header;
